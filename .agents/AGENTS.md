# Project Rules & Guidelines

## Git & Deployment Control
- **Do NOT automatically commit, push, or deploy code changes** after editing files.
- Always perform the code modification locally first, verify it, and wait for explicit instruction from the user before executing `git commit`, `git push`, or deploying.
